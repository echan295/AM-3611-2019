// September 13, 2019

#include <iostream>
#include <cassert>

int main(int argc, char const *argv[]) {
  int a;
  std::cout << "Enter a positive integer. \n";
  std::cin >> a;
  assert(a > 0);
  //  < less than
  // >= greater or equal
  // <= less than or equal
  // != not equal
  // == equal

  // additional notes
  // = is an assignment
  return 0;
}
